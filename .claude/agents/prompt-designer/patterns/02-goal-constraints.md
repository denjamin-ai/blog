---
description: "Pattern: Explicitly define goals and constraints"
enabled: true
---

# 2. Goal & Constraints Pattern

**What**: Explicitly define the goal and constraints for the task.

**When**: For any non-trivial task.

**Application**:
- Clearly define the task objective
- Explicit prohibitions: avoid using `any` without necessity, don't violate architecture
- State what success looks like
