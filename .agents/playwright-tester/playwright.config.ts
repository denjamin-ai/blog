// Алиас: агент запускает тесты через `npx playwright test --config .agents/playwright-tester/playwright.config.ts`
// Все настройки — в корневом playwright.config.ts
export { default } from "../../playwright.config";
